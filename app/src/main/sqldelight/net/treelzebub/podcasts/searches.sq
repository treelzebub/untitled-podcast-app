CREATE TABLE search (
    _id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    query TEXT NOT NULL
);

select_all:
SELECT * FROM search;

insert:
INSERT INTO search VALUES (?, ?);

delete:
DELETE FROM search WHERE ? = _id;